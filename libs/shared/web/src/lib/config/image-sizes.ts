/**
 * Recommended asset dimensions for responsive display.
 * Grid: 2 cols (default) → 3 (sm) → 4 (md). Container max 80rem, aspect 4:3, 2x retina.
 */
export const GAME_THUMB = { width: 640, height: 480 } as const;
